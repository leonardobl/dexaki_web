import styled from "styled-components";

export const Container = styled.div`
  border: 1px solid #ddd;
  padding: 8px 12px;
  border-radius: 4px;
  height: fit-content;
  display: flex;
  align-items: center;
  gap: 16px;

  > button {
    all: unset;
  }
`;
